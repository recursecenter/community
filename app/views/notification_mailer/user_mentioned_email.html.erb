<%= markdown @post.body %>

<div style="border-top: 1px solid #ccc;">
  <p>You received this message because <%= @mentioned_by.first_name %> mentioned you in the post above.</p>
  <p>To reply, either respond to this email or <a href="<%= post_url(slug: @post.thread.slug, thread_id: @post.thread.id, id: @post.id) %>">click here</a>.</p>
</div>
