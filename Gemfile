source 'https://rubygems.org'

ruby '2.2.2'

gem 'rails', '4.2.1'
gem 'pg'

gem 'redis'

gem 'dalli'
gem 'memcachier' # this just sets environmental variables

gem 'delayed_job_active_record'

gem 'skylight'

# Use SCSS for stylesheets
gem 'sass-rails'
gem 'bootstrap-sass', '~> 3.1.1'
gem 'font-awesome-sass'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

gem 'rails_12factor', group: [:development, :production]

gem 'oauth2'
gem 'cancancan', '~> 1.8'

gem 'redcarpet'

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0',          group: :doc

gem 'pry-rails'

group :development do
  gem 'pry-remote'
  gem 'quiet_assets'
  gem 'web-console', '~> 2.0'
end

group :development, :test do
  gem 'jasmine'
  gem 'dotenv-rails'
end

# Use puma as the app server
gem 'puma'
gem 'faye-websocket'
gem 'eventmachine', git: 'https://github.com/recursecenter/eventmachine.git', ref: '97f977072b7e1939770db86e0b3c686999d11cf4'

gem 'em-pg-client'

gem 'thread_safe'

gem 'elasticsearch'
gem 'elasticsearch-model'
gem 'elasticsearch-rails'
