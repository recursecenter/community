machine:
  services:
    - redis
    - elasticsearch
  environment:
    REDIS_URL: redis://localhost:6379
    JASMINE_CONFIG_PATH: test/javascripts/support/jasmine.yml
  pre:
    - curl -k -L -o phantomjs.tar.bz2 https://bitbucket.org/ariya/phantomjs/downloads/phantomjs-1.9.8-linux-x86_64.tar.bz2
    - tar -jxf phantomjs.tar.bz2

checkout:
  post:
    - cd client && lein cljsbuild once test